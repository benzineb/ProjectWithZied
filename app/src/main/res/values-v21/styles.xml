<resources>>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>

    <style name="MyBtn" parent="android:Theme.Material">
        <item name="android:textColor">@color/white</item>
        <item name="android:buttonStyle">@dimen/activity_horizontal_margin</item>
        <item name="android:shape">oval</item>
        <item name="android:shadowColor">@color/wallet_holo_blue_light
            <item name="android:windowActivityTransitions">true</item>

            <!-- specify enter and exit transitions -->
            <item name="android:windowEnterTransition">@transition/explode</item>
            <item name="android:windowExitTransition">@transition/explode</item>

            <!-- specify shared element transitions -->
            <item name="android:windowSharedElementEnterTransition">
                @transition/change_image_transform</item>
            <item name="android:windowSharedElementExitTransition">
                @transition/change_image_transform</item>
        </item>

    </style>

    <style name="MyButton" parent="Theme.AppCompat.Light">
        <item name="android:textColor">@color/white</item>
        <item name="colorControlHighlight">@color/colorPrimary</item>
        <item name="colorButtonNormal">@color/pink</item>
        <item name="android:button">@drawable/mybtn</item>
        <item name="android:buttonStyle">@dimen/activity_horizontal_margin</item>
        <item name="android:shape">oval</item>
        <item name="android:shadowColor">@color/wallet_holo_blue_light</item>

    </style>
</resources>
